# AI Project Assistant 🤖

A full-stack web app that helps you **chat** with an AI and **auto-generate** new project scaffolds.

## 🧠 Features
- Chat window connected to backend AI
- Generate project ZIPs (React + Flask)
- Lightweight & fast (Vite + Flask)

## ⚙️ Run Locally
```bash
# backend
cd backend
pip install -r requirements.txt
python app.py

# frontend
cd frontend
npm install
npm run dev
"# AI-Project-Assistant" 
